module Kooft
{
	a : int;
	k : int;
	b : bool;
	
	test(c : int, d : string) : bool
	{
		k := a;
		a := -k;
		b := not b;
	}
	
	test(c: string, d:string) : bool
	{
		b := true;
		b := a = k;
		b := a >= k;
	}
	
	virtual otherMethod(a: int) : string
	{}
	
};